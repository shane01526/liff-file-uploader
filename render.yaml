services:
  - type: web
    name: your-app-name
    env: node
    buildCommand: |
      apt-get update
      apt-get install -y imagemagick ghostscript
      npm install
    startCommand: node server.js
